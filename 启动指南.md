# 启动指南（2026-09-02 验证通过）

## 项目结构

- `open-notebook/` — 主项目，AI 研究助手（fork 自 lfnovo/open-notebook v1.14.0，有本地改动）
- `agent/` — 外部 deepagents 服务，open-notebook 的 chat 功能会代理到它（端口 5060）
- `langfuse/` — Langfuse 源码副本（可观测性平台，尚未集成）

## open-notebook 启动（Docker 方式）

```bash
cd open-notebook
docker compose up -d --pull never
```

- 验证：`curl http://localhost:5055/docs` 返回 200；UI 在 http://localhost:8502
- 状态/日志：`docker compose ps`、`docker logs -f onv2-open_notebook-1`
- 停止：`docker compose stop`（不要 down，避免网络状态丢失）

生效的配置：`docker-compose.yml` + `docker-compose.override.yml`（自动合并，无需指定 -f）。

override 做的事：
- 项目名 `onv2`，镜像钉死 `1.14.0`，`pull_policy: missing`
- SurrealDB 数据放在 WSL 原生路径 `/home/wangj/onv2_surreal_data`（rocksdb 不能放 /mnt/c，Windows 9p 挂载会认证失败）
- fork 改动通过源码挂载进官方镜像（chat.py / config.py / insights.py / models.py / source_chat.py / context_builder.py）
- chat 代理到宿主机 `host.docker.internal:5060` 的 agent 服务

## agent 服务启动

```bash
cd agent
make dev    # 首次会从 .env.example 生成 .env，需填 LLM_API_KEY；端口 5060
```

需要 chat 功能时必须启动，否则 chat 请求会超时。

## 见解生成的两种触发方式

1. **界面"生成新见解"下拉框**：选"核心观点"（name=agent_extract 的伪转换规则）→
   ON 转调 agent 服务 `/extract`。规则由 seed 脚本创建（幂等，只需跑一次）：

   ```bash
   cd open-notebook && python3 scripts/seed_agent_transformation.py
   # 如 ON 开了密码: --token <ON密码>
   ```

2. **AI 助手对话**：说"提取核心观点"等，chat agent 识别意图后调
   `start_core_viewpoint_extraction` 工具异步启动提取（进度: `GET :5060/jobs/{job_id}`）。

注意：改 agent 代码后需重启 agent 服务（chat agent 是进程级缓存的）；
改挂载的 ON Python 文件后需 `docker compose restart open_notebook`。

## 踩过的坑

### 1. 服务名写错

`docker compose up -d opennotebook` → `no such service`。
正确服务名是 **`open_notebook`**（下划线）。不加服务名则全部启动。

### 2. pull_policy: always + Docker Hub 连不上

主 docker-compose.yml 两个服务都是 `pull_policy: always`，即使本地已有镜像也强制去 Docker Hub 拉取，
当前网络连 registry-1.docker.io 会 TLS handshake timeout，导致启动失败。

解法：启动命令加 **`--pull never`**（override 只把 open_notebook 改成了 `missing`，surrealdb 没改，
所以仍需命令行参数兜底）。

### 3. 旧容器丢失网络连接 → 数据库连不上

症状：应用日志报 `[Errno -2] Name or service not known`，解析不了 `surrealdb` 主机名。
原因：`onv2-surrealdb-1` 旧容器没挂到任何 Docker 网络（`docker inspect` 的 NetworkSettings.Networks 为空），
两个容器不在同一网络，DNS 互相不通。

解法：**`--force-recreate`** 重建容器：

```bash
docker compose up -d --pull never --force-recreate
```

排查方法：对比两个容器的 NetworkID 是否一致：

```bash
docker inspect onv2-open_notebook-1 --format '{{range .NetworkSettings.Networks}}{{.NetworkID}}{{end}}'
docker inspect onv2-surrealdb-1 --format '{{range .NetworkSettings.Networks}}{{.NetworkID}}{{end}}'
```

### 4. SurrealDB 数据不能放 /mnt/c

rocksdb 在 Windows 9p 挂载（/mnt/c）下会认证失败。override 已把数据卷指向
WSL 原生路径 `/home/wangj/onv2_surreal_data`，不要改回 ./surreal_data。

### 5. 加密密钥

`OPEN_NOTEBOOK_ENCRYPTION_KEY` 从 open-notebook/.env 读取（compose 自动加载同目录 .env 做插值）。
API key 在数据库中加密存储，换了密钥旧的加密数据会解不开。
